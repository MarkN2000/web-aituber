# 実装方針

## ファイル構成

開始時点では以下を目安とする。

```text
.github/
└─ workflows/
   └─ release.yml
Cargo.toml
config.example.json
src/
├─ main.rs
├─ config.rs
├─ state.rs
├─ protocol.rs
├─ routes.rs
├─ pipeline.rs
├─ llm.rs
├─ tts.rs
└─ audio.rs

web/
├─ input.html
├─ draw.html
├─ main.html
├─ admin.html
├─ style.css
├─ draw.css
├─ history.css
└─ js/
   ├─ input.js
   ├─ draw.js
   ├─ sources.js
   ├─ main.js
   ├─ admin.js
   ├─ history.js
   ├─ audio-queue.js
   ├─ vrm-viewer.js
   └─ motion.js

assets/
├─ model.vrm
└─ motions/
   ├─ idle.vrma
   └─ その他の待機・感情モーション
```

実際の設定は、`config.example.json`を複製した`config.json`に記載する。`config.json`にはAPIキーと管理用トークンを含むため、Gitの管理対象にしない。公開画面は`event_identifier`を含む`/event/{イベント識別子}`配下で提供し、画面取得、投稿、表示設定取得、WebSocket接続の各入口で現在値との一致を検証する。管理画面は管理用トークンで認証した専用APIから`public_base_url`とイベント識別子を保存し、識別子の変更時だけ既存の公開WebSocketを終了する。無効な識別子の公開画面と、識別子変更を通知された表示中の公開画面は、操作UIを破棄して共通の無効URL表示へ切り替える。公開先ベースURLはQRコードとコピー用URLの生成にだけ使用し、アクセス判定には使用しない。QRコードは管理APIが受け取ったHTTP(S) URLをSVGへ変換し、ブラウザはSVGを`img`としてダイアログへ表示する。イベント識別子は公開URLそのものであり、英小文字、数字、ハイフンからなる1〜64文字として検証する。管理画面はLLMとTTSの運用中に変更する項目だけを取得・保存し、APIキー、管理用トークン、待受設定、FFmpegパス、キャラクター設定をブラウザへ返さない。

表示設定APIは、`/assets/`配下のVRM、モーション、背景画像、BGMについてファイルの更新日時とサイズから安定したバージョンを生成し、URLへ付加する。バージョン付きアセットは長期`immutable`キャッシュ、バージョンなしアセットは再検証を指定する。これにより更新日時とサイズが同じファイルは端末のキャッシュを再利用し、どちらかが変化した場合だけ新しいURLで取得する。外部URLと`/assets/`外のURLは変更しない。

描画画面は、広い画面でキャンバス列と操作盤列を並べ、狭い縦画面では1列、高さの小さい横画面ではキャンバスとカラーUIの最大寸法を画面高に合わせる。カラーUIは1枚のCanvasへ色相リング、中央の彩度・明度領域、各選択位置を描き、Pointer EventsからHSV値を更新する。描画カーソルは描画Canvasと同じ親要素内のオーバーレイとして表示し、Canvasの表示倍率に合わせてブラシ径を変換する。

メイン画面URLに値を問わず`debug`クエリパラメータが存在する場合だけ、デバッグ機能をまとめて有効にする。右手配下の食事アンカーへ座標軸と設定サイズの枠を追加し、画面右上のオーバーレイへWebSocketの接続状態、実際に選択された身体モーションのファイル名と待機・感情の種別、VRMへ要求した基本表情とモデル側の対応状況、食事動作の状態を表示する。接続状態はメイン画面、その他はVRM表示が管理し、メイン画面で一つのデバッグ状態へ合成する。食事動作は画像読込中・表示中・消費中・読込失敗・なしの遷移だけを通知し、フレームごとの進行値はDOMへ反映しない。口パクの口形と瞬きは表示対象にしない。設定変更は管理APIで設定ファイルを再読み込みした後、デバッグ表示中のメイン画面をブラウザで再読み込みして反映する。

管理画面からの設定保存は、読み込み元の設定ファイルを改めて読み、編集可能項目だけを適用して検証する。これにより、画面に出さない項目の外部変更を古い実行中設定で上書きしない。設定ファイルと同じディレクトリの一時ファイルへ全体を書き込んで同期した後、既存ファイルのアクセス権を保ったまま原子的に置換する。Linuxでは同一ファイルシステム内のrename、Windowsでは既存ファイルを置換できるOS APIを使用する。保存に成功してから実行中の設定を切り替え、保存とファイルからの再読み込みは直列に処理する。

ユーザー辞書管理は管理用APIが入力中のTTSエンジンURLへ中継し、VOICEVOX・AivisSpeechで共通する単一語、共通5品詞、優先度、アクセント位置だけを管理画面用の形式へ変換する。辞書はTTSエンジン側だけへ保存し、設定ファイルや実行中状態には保持しない。

ユーザー辞書フォームは読みをブラウザ上でモーラへ分割し、各モーラと右端の平板位置へスナップする1つの`range`入力を表示する。`ァィゥェォャュョヮ`だけを直前の文字へ連結し、その他は1文字を1モーラとする。スライダーの1からモーラ数までは同じ値の`accent_type`、一番右の追加位置は平板を表す`accent_type: 0`へ変換する。高低線はアクセント型から算出した説明用のSVGとし、単語の後ろの位置を加えて平板と語末アクセントの違いを表示する。長い読みではスライダーの内側だけを横スクロールさせる。

ユーザー辞書の試聴では、入力中のカタカナを`POST /audio_query`へ渡し、生成された全アクセント句のモーラを連結した読みを入力と照合してから、モーラを1単語のアクセント句へまとめる。照合時だけ両方の読みへ`ヂ→ジ`、`ヅ→ズ`、`ヲ→オ`、`ヰ→イ`、`ヱ→エ`、`ヮ→ワ`を適用し、それ以外は完全一致を要求する。休止や疑問表現を含む音声クエリは対象外とする。辞書のアクセント位置0は平板を表すため末尾モーラの位置へ変換し、`POST /mora_pitch`で指定位置に対応する音高を再計算してから`POST /synthesis`へ渡す。その他のAudioQuery項目はエンジンの応答をそのまま保持し、ユーザー辞書への追加・更新は行わない。

バックエンドはHTTP・WebSocketから `pipeline` を呼び出し、`pipeline` が `llm`、`tts`、`audio` の処理順序を管理する。フロントエンドの `main` は、音声再生、VRM表示、モーションを各モジュールへ委譲する。

## 分割方針

- 1ファイルには基本的に一つの役割を持たせる。
- 異なる理由で変更される処理が混ざった場合に分割する。
- 同じ処理が実際に重複するまで汎用化しない。
- 行数のみを理由にファイルを分割しない。
- 必要性が確認されていない抽象化、設定、互換処理は追加しない。

ファイル構成とコード量は目安であり、固定の制約とはしない。全体は数千行程度を想定し、規模が増えた場合は仕様にない機能や抽象化が加わっていないか確認する。

## 外部ツールとアセット

- サーバーは`0.0.0.0:3000`で待ち受け、同一ローカルネットワークとCloudflare Tunnelの両方から接続できるようにする。
- WAVからWebM/Opusへの変換にはFFmpegを使用する。
- VRMモデルは `assets/model.vrm` に配置する。
- VRMAは `assets/motions/` に配置する。
- メイン画面は、表示開始ボタンが押された後に音声再生とAudioContextを開始する。
- メイン画面はPage Visibility APIで非表示を検知し、BGMとTTS音声を再生位置と待機列を保持したまま一時停止する。再表示時はAudioContextと各音声を再開する。
- LLMはResponses APIのWeb検索開始イベントだけをフィラー生成の合図にし、最終回答は1回だけ処理する。
- 食事投稿はWeb検索と会話履歴を使用せず、画像の感想生成だけをLLMへ依頼する。
- 食事投稿はVRM表示用の256×256px透過WebPとAI入力用の128×128px白背景WebPを受け付け、前者だけをメモリ内の一時URLで配信してVRMの右手へ追加したQuadへ貼り付け、後者だけをLLMへ送信する。
