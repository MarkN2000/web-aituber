# Web AITuber 仕様書

## 1. 目的

「Web AITuber」は、小規模なイベント会場で来場者がブラウザから質問を投稿し、VRMキャラクターがLLMの回答を音声とモーション付きで返すシステムである。

システムはローカルサーバーで動作する。イベント会場の同一ローカルネットワークとCloudflare Tunnelの両方から接続できる。インターネット経由の接続に固定グローバルIPやポート開放は必要としない。

## 2. システム構成

- ローカルサーバー
  - Rust / axum
  - 入力受付、待ち行列、LLM呼び出し、TTS呼び出し、各画面への配信を担当する
  - Cloudflare Tunnelの接続元となる
- 来場者端末
  - スマートフォンやPCのブラウザを複数台接続できる
  - インターネット経由でCloudflare Tunnelの公開URLへ接続する
  - メイン画面ではVRMと回答を視聴し、質問を投稿できる
  - 簡易入力画面ではテキストを投稿できる
  - 描画画面では食べ物の絵を描いて投稿できる
- 管理端末
  - 管理用トークンを持つブラウザから現在の状態を確認し、回答を中断できる
- 外部・互換API
  - テキスト・画像入力・Web検索に対応したOpenAI Responses API
  - VOICEVOXまたはAivisSpeech

フロントエンドはHTML / CSS / JavaScriptを基本とし、VRM表示にThree.js、`@pixiv/three-vrm`、`@pixiv/three-vrm-animation`を使用する。

## 3. 画面

公開画面は設定項目`event_identifier`を含むイベント別URLだけで提供する。識別子は英小文字、数字、ハイフンからなる1〜64文字とし、先頭と末尾にはハイフンを使用しない。管理画面での案内用URLは設定項目`public_base_url`と識別子を組み合わせて生成する。`public_base_url`はパス、クエリ、フラグメント、認証情報を含まないHTTP(S)のURLとする。`/`、`/input`、`/draw`から現在の公開URLへの転送は行わない。

### 3.1 メイン画面 `/event/{イベント識別子}`

- 認証なしで複数端末から接続できる
- ブラウザの音声再生とAudioContextを有効にする表示開始ボタン
- VRMキャラクター
- LLMの回答テキスト
- 回答文がまだない処理中は、回答表示位置へローディング表示を出す
- 画面下部へ固定する1行の質問入力バー
- 入力バー右端の送信ボタン
- 送信中および送信開始から1秒間は送信ボタンを無効にし、フォームの再送信も受け付けない
- 現在の回答は入力バーの上に配置し、待機中は非表示にする
- 通常の受付完了、待機、生成、再生状態を示す文言は表示せず、送信・生成中のローディング表示と操作に必要なエラーだけを表示する
- 正常完了した直近10件の共有会話履歴
- Web検索を使用した回答の末尾へ地球儀アイコンを表示し、クリック時だけ出典URL一覧を表示する
- 会話履歴は背景や吹き出しを設けず、ユーザー文は細字、AI回答は太字とし、それぞれ異なる色の縁取りを付ける
- 新しい会話を下へ揃え、画面内に収まらない古い会話は上方向へスクロールして確認できる
- 会話履歴の位置、幅、高さ、文字サイズ、縁取りは専用CSSで独立して調整できる

### 3.2 簡易入力画面 `/event/{イベント識別子}/input`

- テキスト入力欄
- 送信ボタン
- 送信中および送信開始から1秒間は送信ボタンを無効にしてフォームの再送信も受け付けず、送信に失敗した場合だけエラーを表示する
- 質問は2,000文字までとする

簡易入力画面には、投稿後の個別チャット画面や会話履歴の表示を持たない。

### 3.3 描画画面 `/event/{イベント識別子}/draw`

- 認証なしで複数端末から接続できる
- 画面上部には「食べ物を描こう」と短い説明だけを表示し、サービス名の補助見出しは表示しない
- 正方形の描画キャンバス
- 横幅が十分な画面ではキャンバスを左、色と描画ツールを右へ配置する。狭い縦画面ではキャンバス、送信ボタン、描画ツールの順に1列で配置し、高さの小さい横画面では左右配置を維持しながらキャンバスと色選択を縮小する。いずれも要素を重ねず、横スクロールを発生させない
- 「1つ戻す」と「全消去」はキャンバスの上、食事投稿の送信ボタンはキャンバスの下へ配置する
- 描画色は、基本色のショートカットと、色相の輪の中央に彩度・明度の四角を置いた常時表示の色選択UIから指定する
- 線の太さは大きさの異なる黒丸で示す3段階のボタンから選ぶ
- ペン、バケツ、消しゴムは常にいずれか一つを選ぶ切り替えUIとし、初期状態はペンとする。バケツ選択中は線の太さを無効にする
- マウスまたはペンでキャンバスを指している間は、ペンでは選択中の太さを示す円、バケツではバケツアイコン、消しゴムでは選択中の太さを示す円と消しゴムアイコンをポインター位置へ表示する。タッチ操作ではポインター表示を出さない
- ペンと消しゴムの入力点には、管理画面で設定した0〜10の手ブレ補正を時間基準で適用する。0は補正なしとし、値が大きいほど入力点を滑らかにする代わりに追従を遅くする。初期値は3とする。バケツには適用しない
- 手ブレ補正値は描画画面の読み込み時に表示設定APIから取得し、管理画面での変更は描画画面の再読み込み後に反映する。描画画面には補正値の操作UIを表示しない
- 「1つ戻す」は、ペンまたは消しゴムの1ストローク、バケツの1回の塗り、全消去をそれぞれ1操作として、直前から最大10操作まで取り消す。描画色、線の太さ、ツールの切り替えは取り消し対象にしない。ボタン操作に加え、Windows/Linuxでは`Ctrl+Z`、macOSでは`Command+Z`でも実行できる。文字入力中、IME変換中、描画操作中、送信中、取り消し履歴がない場合はショートカットを実行しない
- 取り消し履歴はブラウザのメモリ内だけに保持し、履歴がない間は「1つ戻す」を無効にする。送信成功、ページの再読み込み、ページ終了時に履歴を破棄する
- バケツ塗りはクリック地点と同じRGBA値で上下左右につながる領域を、選択中の色で不透明に塗る
- バケツ塗りの領域は周囲8方向へ1px拡張し、拡張部分を選択色で上書きすることで、重ね描きで生じた不透明・半透明の境界色を残さない
- 描いた絵を食事投稿として送るボタン
- 送信中および送信開始から1秒間は送信ボタンを無効にして再送信も受け付けず、受付結果またはエラーを表示する
- 描画キャンバスは512×512pxとし、白塗り処理後に品質75%のWebP画像を2枚生成する。VRM表示用は256×256pxで未描画部分と消しゴム部分の透過を保持し、AI入力用は128×128pxで透明部分を白く合成した完全不透明画像とする
- 食事投稿APIにはVRM表示用画像とAI入力用画像を同じmultipartリクエストで送り、どちらか一方でも欠ける投稿は受け付けない
- 送信時はアルファ128以上を線として半径2pxのクロージングを行い、外周から到達できない透明領域だけを白・不透明で埋める
- 隙間補完と白埋めは送信用画像だけに適用し、編集用キャンバスと元の線は変更しない
- 食事投稿には質問文や料理名の入力欄を設けない

描画画面には、投稿後の個別チャット画面や会話履歴の表示を持たない。

### 3.4 管理画面 `/admin`

- 設定された管理用トークンを持つ端末だけが接続できる
- 「運用」「AI設定」「音声設定」「表示設定」の4タブで構成する
- 運用タブには、待機中、生成中、食事演出中、発話中などの状態と、現在の処理を中断して次の投稿へ進む操作を表示する
- 運用タブでは公開先ベースURLとイベント識別子を入力し、識別子はランダム生成できる。メイン、簡易入力、描画の公開URLを確認・コピーできる
- 各公開URLにはQRコード表示ボタンを設け、押した場合だけ画面名、読み取り可能な大きさのQRコード、URLをダイアログへ表示する。QRコードに管理用トークンを含めない
- 公開先ベースURLの変更は管理画面で案内するURLだけを変更し、接続中の公開画面やイベント識別子の有効性には影響させない
- 公開URLの変更前に確認を表示し、変更後は以前の識別子を直ちに無効にする。以前の公開URLから新しいURLへの転送や、新しい識別子の表示は行わない
- 公開URLの変更では、待ち行列、処理中の投稿、共有会話履歴を消去しない
- 管理画面には現在の質問文とLLMの回答文を表示しない
- AI設定タブでは、LLM APIのURL、モデル名、通常質問のシステムプロンプト、食事反応プロンプト、検索中フィラー文の一覧を編集し、確認後に共有会話履歴を削除できる
- 音声設定タブでは、TTSエンジンのURLと使用する話者・スタイルを編集する。話者IDは画面へ表示しない
- 音声設定タブでは、入力中のTTSエンジンURLから話者とスタイルの一覧を取得し、一覧から使用するスタイルを選択する
- 各設定タブの保存操作は、そのタブで編集した項目だけを保存し、他方のタブで保存していない入力内容を変更しない
- 管理画面で編集した設定は、サーバーで検証し、読み込み元の設定ファイルへ安全に保存してから実行中の設定へ反映する
- 保存に成功した設定は次に処理を開始する投稿から反映し、検証または保存に失敗した場合は設定ファイルと実行中の設定を変更しない
- 音声設定タブでは、保存前の入力値を使用して固定の短文を試聴できる。試聴は設定を保存しない
- 表示設定タブでは、準備中画像のアップロード・上書き・削除と、`character.preparation_mode`による準備中モードのON/OFFを操作できる
- 選択した準備中画像は背景画像と同様に管理端末のブラウザで長辺1920px以内へ縮小し、品質85%のWebPへ変換する。元画像と変換後画像はそれぞれ10MiB以下とし、サーバーは`assets/preparation.webp`の固定ファイルとして原子的に保存する
- 準備中画像が未設定でも準備中モードをONにできる。ONの間も準備中画像のアップロード・上書き・削除を許可し、変更はその場で反映する
- 準備中モードをONにした場合は進行中の投稿を中断し、待ち行列にある投稿を処理せずキャンセルする。通常質問と食事投稿の新規受付は`503 Service Unavailable`と「現在は準備中です」のエラーで拒否し、LLMとTTSを呼び出さない
- 準備中のメイン画面は準備中画像を画面内に収めて表示し、画像が未設定の場合は黒一色で表示する。VRM、モーション、食事画像、背景、画面オーバーレイ、会話履歴、入力UI、デバッグ表示、TTS音声を表示・再生しない。BGMと音声コンテキスト、設定変更を受け取るWebSocket接続は維持する
- メイン画面を開いた直後の表示・音声開始操作は通常時と準備中で共通とする。準備中モードと準備中画像の変更に成功した場合は接続中のメイン画面へ通知し、開始画面へ戻さずその場で反映する
- 表示設定タブでは、`character.vrm_url`が`/assets/model.vrm`の場合に、端末内の100MiB以下のVRMモデルを選択して現在のモデルと置き換えられる。モデル一覧、3Dプレビュー、削除、履歴、バックアップは設けない
- サーバーはアップロードされたファイルのGLB構造とVRM 0.xまたはVRM 1.0の拡張情報を検証し、成功した場合だけ`assets/model.vrm`を原子的に置き換える。失敗した場合は現在のモデルを維持する
- VRMモデルの置き換えに成功した場合は接続中のメイン画面へ変更を通知する。メイン画面が発話または食事動作を処理中の場合は、現在の処理が終わってから新しいモデルを読み込む
- 表示設定タブではモデルの明るさを0〜200%のスライダーで設定する。100%を基準とし、設定値は`character.light.brightness`へ0.0〜2.0の倍率で保存して主光源と環境光の強度へ同じ倍率を適用する
- 明るさの保存に成功した場合は接続中のメイン画面へ変更を通知する。メイン画面が発話または食事動作を処理中の場合は、現在の処理が終わってから照明を読み込み直す
- 表示設定タブではモデルのアンチエイリアスをON/OFFで設定し、`character.antialias`へ真偽値で保存する。初期値はONとする
- アンチエイリアスがONの場合は、VRMの`alphaTest`が0より大きいマテリアルへ`alphaToCoverage`を適用する。OFFの場合と`alphaTest`が0のマテリアルには適用しない
- アンチエイリアスの保存に成功した場合は接続中のメイン画面へ変更を通知する。メイン画面が発話または食事動作を処理中の場合は、現在の処理が終わってからビューアーを再初期化する
- 表示設定タブではCameraのPosition X・Y・Zと、Food PropのPosition X・Y・Z、Rotation X・Y・Z、Scaleを数値で設定し、一つの保存操作で確定する。CameraのTargetとFOVは変更しない
- 位置調整の保存に成功した場合は接続中のメイン画面へ変更を通知する。メイン画面が発話または食事動作を処理中の場合は、現在の処理が終わってから配置を読み込み直す
- 表示設定タブでは、現在の背景画像を確認し、端末内のJPEG、PNG、WebP画像を選択して背景画像としてアップロード・上書きできる
- 選択した背景画像は管理端末のブラウザで長辺1920px以内へ縮小し、品質85%のWebPへ変換する。元画像と変換後画像はそれぞれ10MiB以下とする
- サーバーは変換後画像を`assets/background.webp`の固定ファイルとして保存する。任意のファイル名や保存先は指定できない
- 表示設定タブから背景画像を削除できる。背景画像がない場合は設定ファイルの背景色を表示する
- 背景画像のアップロード、上書き、削除に成功した場合は、接続中のメイン画面へ変更を通知してその場で反映する
- 表示設定タブでは、背景画像とは別に、画面オーバーレイを左上・右上・左下・右下の4固定スロットで管理する。各スロットには画像を1枚だけ設定でき、現在の画像の確認、端末内のJPEG、PNG、WebP画像の選択によるアップロード・上書き、削除、表示倍率の保存ができる
- 画面オーバーレイの選択画像は、背景画像と同様に管理端末のブラウザで長辺1920px以内へ縮小し、品質85%のWebPへ変換する。元画像と変換後画像はそれぞれ10MiB以下とする。透過は変換後も保持する
- サーバーは各スロットの変換後画像を`assets/screen-overlay-top-left.webp`、`assets/screen-overlay-top-right.webp`、`assets/screen-overlay-bottom-left.webp`、`assets/screen-overlay-bottom-right.webp`の固定ファイルとして保存する。任意のファイル名や保存先は指定できない
- 画面オーバーレイの表示倍率は、画面全体を覆う大きさを100%とする1〜100%の整数で、初期値は100%とする。倍率は各スロットごとに保存する。ON/OFF、不透明度、自由座標、並べ替えの設定は設けない
- 画面オーバーレイのアップロード、上書き、削除、表示倍率の保存に成功した場合は、接続中のメイン画面へ変更を通知してその場で反映する
- 表示設定タブでは、現在のBGMを試聴し、端末内のMP3、OGG、WAV音源を選択してBGMとしてアップロード・上書きできる
- BGMのアップロード元ファイルは100MiB以下とし、サーバーは一時ファイルへストリーミング保存する。アップロード全体をメモリへ保持しない
- サーバーはアップロードされたBGMをFFmpegで48kHz、ステレオ、128kbpsのWebM/Opusへ変換し、`assets/background-music.webm`の固定ファイルとして保存する。音量正規化は行わない
- BGMの変換に成功した場合だけ現在のファイルを原子的に置き換え、変換に失敗した場合は現在のBGMを維持する
- 表示設定タブからBGMを削除し、通常再生時の音量と発話中の音量を0〜100%で設定できる。通常再生時の初期値は30%、発話中の初期値は通常再生時に対して40%とする
- BGMの通常音量は設定ファイルの`character.background_music_volume`、発話中の比率は`character.background_music_duck_ratio`へ、それぞれ0.0〜1.0で保存する。管理画面は`PUT /api/admin/background-music-volume`へ`{"volume": 0.3, "duck_ratio": 0.4}`形式で送り、サーバーは他の設定項目を変更せず両方の音量設定だけを保存する
- BGMのアップロード、上書き、削除、音量変更に成功した場合は、接続中のメイン画面へ変更を通知してその場で反映する
- 外部で設定ファイルを編集した場合のため、設定ファイルを検証して再読み込みする操作を残す
- 設定の再読み込みに成功した場合は次に処理を開始する投稿から反映し、失敗した場合は現在の設定を維持する
- 待受アドレスとポートの変更だけは再起動後に反映する
- 設定の再読み込みに成功した場合は、接続中のメイン画面へ表示設定の変更を通知する。背景とBGMはその場で反映し、VRM、モーション、カメラ、照明、食事画像の配置は待機中ならすぐ、処理中ならその処理の終了後にビューアーを再初期化して反映する
- 運用タブには現在のアプリケーションバージョンとアップデート確認ボタンを表示する。更新がある場合は対象バージョンを示して確認を取り、同意後に更新して再起動する
- 管理画面からの自己更新は、正式な配布フォルダから単体起動している場合だけ許可する。ソースからの開発起動とsystemdを含む外部プロセス管理下では更新の有無だけを表示し、更新処理は開始しない
- 自己更新では同じOS・CPU向けのGitHub Releaseを取得し、チェックサムと配布物の構成を検証して展開を完了してからサーバーを終了する
- Unixでは、外部アップデーター自身が起動直後に新しいセッションへ移り、サーバー側も外部アップデーターの実行前に新しいセッションへ分離する。これにより旧版からの更新と今後の更新のどちらでも、サーバー終了時に端末から送られるSIGHUPの対象外とする
- 自己更新でサーバーを終了するときは接続中のWebSocketを閉じる。外部アップデーターはサーバープロセスの終了を10秒待ち、残っている場合はそのプロセスを強制終了し、終了を確認してからファイル置換へ進む。管理画面は再起動確認の通信を3秒で打ち切り、更新開始から120秒で成功または失敗を確定する
- 外部アップデーターは実行ファイル、アップデーター、`web`、`config.example.json`、`README.md`だけを置き換え、`config.json`と`assets`を保持する。置き換えまたは新しいサーバーの起動に失敗した場合は旧版へ戻して再起動する
- 外部アップデーターを起動するたびに配布フォルダの`update.log`を上書きし、親プロセスの終了待ちと強制終了、ファイル置換、新サーバー起動、ロールバック、失敗理由を記録する

## 4. 投稿と処理

1. 入力端末から投稿を受け付ける。
2. 投稿をサーバー上のFIFO待ち行列へ追加する。
3. サーバーは投稿を1件ずつ処理する。
4. システムプロンプト、LLM呼び出し直前の現在日時、直近の会話履歴、今回の投稿をResponses APIへ送信する。Web検索の使用はLLMが判断する。
5. Web検索の開始を検知した場合だけ、設定済みの短いフィラー文の一覧から1文を順番に選び、TTSで生成して音声配信する。フィラーは画面、回答本文、会話履歴へ表示しない。
6. 検索結果を踏まえた最終回答を1回だけ受け取り、感情タグを除いた本文を最大300文字かつ最大4文に制限してから、文単位に分割して各文のTTS音声を生成する。
7. WAV音声を文単位のWebM/Opusへ変換し、フィラーから最終回答まで連続した連番を付ける。
8. 回答テキスト、感情、モーション、音声URL、音声時間、回答種別、出典を各接続端末へ連番順に配信する。
9. メイン画面は音声を取得してローカルの再生キューへ追加し、連番順に再生する。
10. サーバーはフィラーを含む音声時間を基準に回答の進行を管理し、メイン画面からの再生完了通知を待たずに次の投稿へ進む。

- 接続中の全入力端末で共通の、正常完了した直近10件の質問文と回答文を会話履歴として使用する。
- 失敗した投稿と中断した投稿は会話履歴へ保存しない。
- 検索を使用した回答では出典URLを会話履歴に保存する。URLは通常の回答本文とTTSへ含めない。
- 会話履歴はサーバーのメモリ内だけに保持し、サーバー再起動時に消去する。
- 管理画面から会話履歴を削除した場合は、保存済みの履歴だけを空にして接続中のメイン画面へ反映する。現在処理中の投稿と待ち行列は変更せず、削除後に正常完了した投稿は新しい履歴として保存する。
- 待ち行列は100件までとし、満杯の場合は時間を置いて再送するよう案内する。
- サーバーが同時に処理する投稿は1件だけとし、各メイン画面が同時に再生する音声も1件だけとする。
- 接続中のすべてのメイン画面へ同じ回答と状態を一斉配信し、管理画面へ状態を配信する。
- 各メイン画面は独立した再生キューを持ち、端末間の厳密な再生同期は行わない。
- メイン画面または管理画面が途中参加・再接続した場合、サーバーは現在の処理状態と正常完了した会話履歴を送信する。管理画面は受信した質問と会話履歴を表示しない。配信済みの音声は再送しない。
- メイン画面の切断や再生遅延は、他の端末とサーバーの進行へ影響させない。
- 中断操作は管理画面からだけ受け付け、現在の投稿IDを含める。サーバーは対象の投稿が処理中の場合だけ中断し、すべての接続端末へ中断を通知する。同じ投稿に対する重複した中断操作は無視する。
- LLMまたはTTSでエラーが発生した場合は接続端末へ通知し、次の投稿へ進む。
- 回答の文字数は感情タグを除いた表示・読み上げ本文のUnicode文字数で数え、文末は`。`、`！`、`？`、`!`、`?`とする。改行は文数に含めない。
- 回答は先頭から文末単位で採用し、次の文を加えると300文字または4文を超える場合は残りを使用しない。先頭の1文だけで300文字を超える場合は回答生成の失敗として次の投稿へ進む。
- 公開画面の取得、質問投稿、食事投稿、表示設定取得、公開WebSocket接続では、URLのイベント識別子が現在の設定と一致することをサーバーで検証する。
- 公開URLを変更した場合は接続済みの公開WebSocketへ終了を通知して切断する。以前の公開URLでは、メイン画面、簡易入力画面、描画画面の操作UIを表示せず、「このURLは使用できません。」だけを表示する。管理画面の接続は維持する。

### 4.1 食事投稿

- 食事投稿は通常の質問と同じFIFO待ち行列へ追加し、同時に1件だけ処理する。
- `character.food_motion`が未設定の場合もサーバーと通常質問は利用できる。食事投稿は`503 Service Unavailable`と「食事モーションが設定されていません」で拒否し、LLM・TTS・画像だけの食事演出は開始しない。待ち行列の食事投稿も処理開始時の設定で確認する。
- 食事投稿の表示用質問文は「食べ物の絵を送りました」とする。
- 食べ物かどうかや内容による事前判定は行わず、正常に受け付けた画像は必ず食事演出の対象とする。
- LLMは画像を見て、設定された食事反応プロンプトに従って実際に食べたような感想を生成する。
- 食事投稿のLLM呼び出しではWeb検索と会話履歴を使用しない。
- LLMの感想とすべてのTTS音声を準備してから、食事演出と発話を連続して配信する。これは公開可否の確認処理ではない。
- メイン画面は投稿画像を貼ったQuadをVRMの右手へ表示し、右手の動きへ追従させる。
- 食事演出の開始時に、`character.food_motion.url`で指定した食事専用VRMAを待機モーションから切り替えて1回再生する。食事VRMAは身体の回転だけを使用し、モデル全体の移動、表情、視線のトラックは適用しない。
- `character.food_motion.consume_at_ms`は演出開始からQuadの消去を始めるまでのミリ秒数とし、右手が口元へ来る時刻に合わせて設定する。消去は不透明度を変えず400ミリ秒かけてスケールだけを縮小する。
- `character.food_motion.duration_ms`は食事演出全体のミリ秒数とし、VRMAの再生時間に合わせて設定する。再生速度は変更しない。演出終了時にはQuadを消去して待機モーションへ戻し、感想の表示と音声再生を開始する。
- 食事VRMAが演出時間より先に終了した場合は演出終了まで最後の姿勢を保持する。食事中は待機・感情モーションで上書きしない。
- 食事投稿の感想では身体の感情モーションを再生せず、待機モーションと表情、瞬き、口パクを使用する。
- 食事VRMAを読み込めない場合は設定エラーを表示し、画像だけの食事演出は行わない。
- 管理画面から食事投稿を中断した場合は、Quadを消去し、食事演出と未再生音声を停止する。
- LLMまたはTTSの失敗、管理画面からの中断では食事演出を開始しない。
- 食事投稿は正常完了時に共有会話履歴へ保存する。質問文は表示用の固定文、回答文は生成した感想とし、画像本体は保存しない。

## 5. キャラクター表示

- イベントごとに使用するVRMモデルを1体設定する。
- モデルとモーションはイベント前に組み合わせを確認する。
- カメラ、背景、照明は設定された固定値を使用する。
- 背景画像がある場合は画面全体へ中央を基準に表示し、アスペクト比を維持したまま縦横いずれかを画面へ合わせ、はみ出す部分だけを切り取る。
- 背景画像がない場合は設定された背景色を使用する。
- 画面オーバーレイはVRMより前、会話履歴、入力UI、デバッグ表示などの画面UIより後ろに表示する。ポインター操作は妨げない
- 各画面オーバーレイは、スロットに対応する画面の隅を基準に、アスペクト比を維持した`cover`表示で画面全体を覆う大きさを決める。保存した表示倍率に従い、その隅を固定したまま等比縮小する。画像がないスロットは表示しない
- 複数の画面オーバーレイは左上、右上、左下、右下のDOM順で重ね、後のスロットほど前面に表示する
- BGMが設定されている場合は、メイン画面の「表示・音声を開始」操作を起点に各表示端末で独立してループ再生する。端末間の再生位置は同期しない。
- BGMの通常音量には設定値を使用し、TTS音声の再生中は通常音量に設定された発話中比率を掛けた音量まで250msかけて下げ、TTS音声の終了後は通常音量まで250msかけて戻す。
- TTSの中断、エラー、別の投稿への切り替え、音声キューの破棄でもBGMを通常音量へ戻す。連続するTTS音声の境界では進行中の音量変更を現在値で打ち切って次の変更を開始する。
- BGMの音量制御にはWeb Audio APIのGainNodeを使用する。BGMを再生できない場合もVRM表示、TTS音声、投稿受付は継続し、メイン画面へBGMのエラーを表示する。
- BGM用AudioContextの生成と再開は「表示・音声を開始」操作から最初の非同期待機前に開始する。ページ表示だけではBGMを再生しない。
- 表示開始後にメイン画面が非表示になった場合は、その端末のBGMと再生中のTTS音声を再生位置を保持して一時停止し、待機中のTTS音声も再生を開始しない。回答テキストと共有会話履歴の更新は継続する。
- メイン画面を再表示した場合は、BGMとTTS音声を一時停止した位置から再開し、待機中のTTS音声を続けて再生する。非表示中に別の投稿への切り替え、中断、エラーが発生した場合は、表示中と同じく対象の再生中・待機中音声を破棄する。
- メイン画面の再表示時に音声を自動再開できない場合も、VRM表示、投稿受付、回答テキストと共有会話履歴の更新は継続し、対象の音声エラーを表示する。
- VRMモデル、モーション、背景などのアセットは、イベントで利用可能なライセンスのものを使用する。

## 6. 表情とモーション

身体のモーションと、顔の表情・瞬き・口パクは独立して制御する。

### 6.1 待機モーション

- body-onlyのVRMAをループ再生する。
- 待機モーションは1種類以上を設定できる。複数設定した場合は、ループ終了時などにランダムで切り替える。
- 切り替え時は短くクロスフェードする。
- キャラクターが移動しないよう、腰の位置を移動するトラックは使用しない。

### 6.2 瞬き

- 基本表情が`neutral`の間だけ、数秒間隔のランダムなタイミングで自動的に行う。
- 待機中と発話中の両方で動作し、`neutral`以外の表情へ切り替えた場合は進行中の自動瞬きを解除して停止する。

### 6.3 表情

- 基本表情は `neutral`、`happy`、`sad`、`angry`、`surprised` とする。
- `neutral`を要求する場合は、VRMモデルの`neutral`表情を100%適用する。モデルに`neutral`表情が存在しない場合だけ全表情を解除する。待機中、感情タグがない発話、`[neutral]`の発話、不正な感情タグのフォールバックで共通してこの規則を使用する。
- LLMは必要に応じて、文頭に `[happy]` などの固定された感情タグを付ける。
- 感情タグは画面表示とTTSへ渡す文章から取り除く。
- タグがない場合や不正な場合は `neutral` とする。
- VRMモデルに対応する表情が存在しない場合は、表情を解除する。
- 対応する音声の再生開始時に表情を切り替え、回答終了後はモデルの`neutral`表情、またはモデルに存在しない場合は全表情を解除した状態へ戻す。

### 6.4 口パク

- メイン画面のブラウザで、再生中の音声をリアルタイムに解析する。
- RMS音量から無音と口の開き具合を判定し、最大開口を抑える。
- FFTの周波数分布から母音を近似し、VRMの `aa`、`ih`、`ou`、`ee`、`oh` 表情を切り替える。
- 母音判定と開閉値を平滑化し、細かなちらつきや急激な動きを抑える。
- TTSエンジン固有の音素情報や、音声ごとの学習データは使用しない。

### 6.5 感情モーション

- 特定の感情と、事前に登録したbody-onlyのVRMAを対応付ける。
- 回答内で最初に現れた登録済みの感情に対応するモーションを、該当する音声の再生開始時に再生する。
- 感情モーションは1回だけ再生し、終了後に待機モーションへ戻る。
- 身体のモーションは1回の回答につき最大1回とする。
- 必要な感情だけ登録し、未登録の感情ではモーションを再生しない。
- LLMが任意のモーション名を指定する方式は使用しない。

### 6.6 食事用の画像

- 投稿画像をテクスチャとして貼った両面の正方形Quadを使用する。
- Quadは半透明合成を使わず、アルファテストの閾値0.5で透過部分を破棄する。
- VRMのHumanoidから`rightHand`の生ボーンを取得し、その子へ実行時にQuad用アンカーを追加する。
- アンカーの位置、回転、大きさは右手のローカル座標を基準としたキャラクター設定値を使用する。
- メイン画面URLに値を問わず`debug`クエリパラメータが存在する場合だけ、すべてのデバッグ表示を有効にする。
- デバッグ表示では、食事画像アンカーへローカル座標軸と設定サイズの枠を追加し、画面右上へWebSocketの接続状態、現在の身体モーションのファイル名と種別（待機・感情・食事）、要求表情とVRMの対応状況、食事動作の状態を表示する。
- 接続状態は接続中・接続済み・再接続中を表示する。
- モーションが再生されていない場合はその旨を表示する。要求表情はVRMへ実際に適用した基本表情を表し、口パクの口形と瞬きは含めない。表情対応はモデル内の表情を適用した場合は対応、表情を解除した`neutral`は基本状態、その他は未対応として表示する。
- 食事動作は、なし・画像読込中・表示中・消費中・読込失敗を表示する。
- `debug`クエリパラメータがない通常のメイン画面には、デバッグ表示を出さない。
- 配置設定の変更は、管理画面で設定ファイルを再読み込みした後、待機中ならすぐ、処理中ならその処理の終了後にデバッグ表示中のメイン画面へ反映する。
- 専用スロットや追加ボーンをVRMモデルへ要求しない。
- 右手を取得できない場合はメイン画面へ設定エラーを表示し、別のボーンへはフォールバックしない。
- 食事VRMAでも同じアンカーを使用し、モーションは右手を口へ運ぶ身体の動きを担当する。

## 7. LLM・TTS

### 7.1 LLM

- テキスト、画像入力、Web検索に対応したOpenAI Responses APIを使用する。
- `web_search`を自動選択で有効にし、検索が必要かどうかはLLMに判断させる。
- 推論量は `low`、回答の詳細度は `low` とし、応答時間、API利用量、読み上げ時間を抑える。
- Responses APIの`max_output_tokens`は固定で1024とし、推論トークンを含む異常な長出力の外側の上限とする。
- システムプロンプトへ、回答本文を日本語で300文字以内かつ最大4文とし、各文を文末記号で終え、Markdownの箇条書きを使用しない指示を追加する。
- `max_output_tokens`到達で応答が未完了になった場合は、受信済み本文のうち文末まで完成した文だけを使用する。完成した文がない場合、または別の理由で応答が未完了になった場合は回答生成の失敗として次の投稿へ進む。
- Web検索は低コンテキストで、1回の回答につき最大1回とする。
- 利用するAPIのURL、APIキー、モデル名、システムプロンプト、検索中フィラー文の一覧を設定できる。
- 食事投稿専用の反応プロンプトを設定でき、通常質問のシステムプロンプトと組み合わせて使用する。
- APIキーを除くLLM設定は管理画面から編集できる。
- LLM APIのURLはHTTPまたはHTTPSとし、URL内にユーザー名やパスワードを含めない。
- モデルを変更する場合もResponses APIとWeb検索に対応したモデルを使用する。
- LLMを呼び出す直前の日本時間をRFC 3339形式で生成し、システムメッセージの末尾へ追加する。
- 現在日時はAIが「今日」「今」などを判断するためだけに使用し、会話履歴へ保存しない。
- 食事投稿は画像の感想生成に必要な1回だけLLMを呼び出し、事前の分類や公開可否判定のための追加呼び出しは行わない。

### 7.2 TTS

- VOICEVOXまたはAivisSpeechを使用する。
- `POST /audio_query`と`POST /synthesis`を使用し、WAV音声を生成する。
- 生成したWAV音声は、モノラル32kbpsのWebM/Opusへ変換する。
- 利用するエンジンのURLと話者IDを設定できる。
- TTSエンジンのURLと使用する話者・スタイルは管理画面から編集できる。選択したスタイルの話者IDを設定値として保存する。
- TTSエンジンのURLはHTTPまたはHTTPSとし、URL内にユーザー名やパスワードを含めない。
- 管理画面から話者一覧を取得する場合は、入力中のTTSエンジンURLの`GET /speakers`を呼び出し、話者名、スタイル名、話者IDだけを管理端末へ返す。取得操作は設定を保存しない。
- 管理画面からの試聴では、入力中のTTSエンジンURLと話者IDを使用して固定文を生成し、WAVを保存せずに管理端末へ返す。
- 管理画面の音声設定から、入力中のTTSエンジンURLに保存されているVOICEVOX・AivisSpeech共通形式のユーザー辞書を読み込み、単語の追加、編集、削除を行える。辞書操作はTTS設定を保存せず、TTSエンジン側の辞書を直接変更する。
- ユーザー辞書で編集できる項目は、単語、カタカナの読み、アクセント位置、優先度とし、品詞は両エンジンで共通する固有名詞、普通名詞、動詞、形容詞、接尾辞に限る。読みへ入力したひらがなは、IMEでの入力確定後にカタカナへ変換する。単一の読みとアクセント位置で表現できないAivisSpeech固有の複合語と、共通外の品詞は一覧と編集の対象にしない。
- ユーザー辞書の追加・編集フォームでは、入力中の読みとアクセント位置を使用し、音声設定で選択中の話者で保存前に試聴できる。試聴はTTSエンジンの辞書を変更せず、入力したカタカナから生成した音声クエリのモーラを1単語のアクセント句へまとめ、指定したアクセント位置に対応する音高を再計算してWAVを生成する。入力と音声クエリの読みを照合するときだけ、TTSエンジンが同音表記へ変換する`ヂ→ジ`、`ヅ→ズ`、`ヲ→オ`、`ヰ→イ`、`ヱ→エ`、`ヮ→ワ`を同じ読みとして扱い、その他の表記差は拒否する。
- ユーザー辞書のアクセント位置は、モーラ位置へスナップする1つのスライダーで指定する。`ァィゥェォャュョヮ`は直前の文字と同じモーラ、`ン`、`ッ`、`ー`とその他のカタカナは1文字を1モーラとして表示する。各モーラの停止位置はその直後で音が下がるアクセント位置とし、一番右の追加位置は平板とする。各停止位置はアクセント欄の幅へ均等に収める。選択したアクセント位置から求めた高低パターンを単語の後ろまで含む簡易線で表示し、平板と語末アクセントを区別できるようにする。この線はTTSエンジンが計算した個別の音高値を表すものではない。
- ユーザー辞書画面には、辞書の読み込み、単語の追加、編集、削除だけを設け、検索、一括操作、インポート、エクスポートは設けない。TTSエンジンURLを変更した場合は辞書を読み込み直すまで編集できない。
- ユーザー辞書はTTSエンジンを正とし、Web AITuberの設定ファイルには複製しない。同じTTSエンジンを使用する他のアプリにも変更が反映されることを管理画面に表示する。

## 8. 通信

- サーバーは`0.0.0.0:3000`で待ち受け、同一ローカルネットワークの端末はサーバーPCのローカルIPv4アドレスへ接続する。
- Windowsファイアウォールでは、プライベートネットワークに限りTCP 3000番への受信を許可する。
- メイン画面、簡易入力画面、描画画面、投稿API、表示設定、一時食事画像、音声、アセットはCloudflare TunnelのHTTPS URLで認証なしに公開する。
- 管理画面、管理用WebSocket、公開イベントリンク管理API、管理用中断API、管理用設定取得・保存API、管理用表示設定API、VRMモデル管理API、背景画像管理API、画面オーバーレイ管理API、BGM管理API、TTS話者一覧API、TTS試聴API、TTSユーザー辞書管理API、設定再読み込みAPI、更新確認・実行APIには、設定された管理用トークンを必要とする。
- 投稿はHTTP `POST`で受け付ける。
- メイン画面への状態、回答、音声メタデータの一斉配信にはイベント識別子を検証する受信専用の公開WebSocketを使用し、管理画面は管理用トークンを検証する別URLのWebSocketを使用する。
- 表示設定が変更された場合は同じWebSocketで変更通知だけを配信し、メイン画面は通知時とWebSocket再接続時に限ってイベント配下の表示設定APIを再取得する。定期取得は行わない。
- 会話履歴は接続時のスナップショットと、正常完了時の更新イベントで配信する。
- 食事演出の開始時に、投稿ID、一時画像URL、Quadの消去開始時刻、演出時間を配信する。
- 音声メタデータには、投稿ID、連番、表示文、回答種別、感情、モーション、音声URL、音声時間、出典を含める。
- TTSから生成したWebM/Opus音声はサーバーが一時保持し、メイン画面がCloudflare Tunnel経由のHTTP `GET`で取得する。
- TTSから受け取ったWAVはメモリ上で処理し、ファイルとして保存しない。
- 食事投稿の画像は待ち行列と演出用のメモリ内だけに保持し、ファイルとして保存しない。一時画像URLは推測困難な投稿IDを使用し、食事演出の開始から5分後に削除する。
- 回答用のWebM/Opus音声は回答終了から5分後に削除し、正常終了時は現在の一時音声フォルダ、次回起動時は異常終了などで残った過去の一時音声フォルダを削除する。
- メイン画面は音声メタデータの受信後すぐに音声を取得し、連番順に再生キューへ格納する。
- メイン画面は音声の再生終了を端末内で検知し、再生キュー内の次の音声へ進む。サーバーへ再生完了は通知しない。
- APIキーなどの秘密情報はサーバー側だけで保持し、ブラウザへ送信しない。
- 管理用設定取得APIは編集可能な項目だけを返し、LLM APIキー、管理用トークン、その他の非編集項目を返さない。
- 管理画面と管理用APIの応答はキャッシュを禁止し、管理画面から他ページへ管理用URLをリファラーとして送信しない。
- 会話履歴の削除は管理用トークンを必要とする`DELETE /api/admin/conversation-history`で行い、成功時は204を返して空の履歴を配信する。
- QRコード生成APIは管理用トークンを必要とし、HTTP(S)の公開URLだけをSVGへ変換してキャッシュを禁止する。
- TTS試聴音声はキャッシュせず、一時ファイルとして保存しない。
- VRMモデル管理APIは100MiB以下の`.vrm`ファイルだけを受け付け、GLB 2.0のチャンク構造とJSONチャンク内の`VRM`または`VRMC_vrm`拡張を検証してから、同じディレクトリ内の一時ファイルを使用して`assets/model.vrm`を原子的に置き換える。
- モデル明るさ管理APIは管理用トークンを必要とする`PUT /api/admin/model-brightness`で`{"brightness": 1.0}`形式を受け付け、0.0〜2.0の有限値だけを保存する。
- モデルのアンチエイリアス管理APIは管理用トークンを必要とする`PUT /api/admin/model-antialias`で`{"antialias": true}`形式を受け付け、他の設定項目を変更せず`character.antialias`だけを保存する。
- モデル配置管理APIは管理用トークンを必要とする`PUT /api/admin/model-layout`でCamera Position、Food Prop Position、Rotation、Scaleを受け付け、有限値かつ正のScaleだけを保存する。CameraのTarget、FOVと他の設定項目は変更しない。
- 手ブレ補正管理APIは管理用トークンを必要とする`PUT /api/admin/drawing-stabilization`で`{"stabilization": 3}`形式を受け付け、0〜10の整数だけを、他の設定項目を変更せず`drawing.stabilization`へ保存する
- 背景画像管理APIは10MiB以下のWebPだけを受け付け、同じディレクトリ内の一時ファイルへ書き込んだ後に`assets/background.webp`を原子的に置き換える。
- 表示設定APIは背景画像が存在する場合だけ、ファイルの更新日時とサイズから生成する更新識別子を付加した同一オリジンの`/assets/background.webp` URLを返す。更新日時とサイズが同じ間は同じURL、どちらかが変わった場合は異なるURLを返す。
- 画面オーバーレイ管理APIの`{slot}`は、左上・右上・左下・右下を表す`top-left`、`top-right`、`bottom-left`、`bottom-right`だけを受け付ける。アップロードは`POST /api/admin/screen-overlays/{slot}`のmultipartフィールド`image`、削除は同じURLの`DELETE`とし、成功時は204を返す。10MiB以下のWebPだけを受け付け、同じディレクトリ内の一時ファイルへ書き込んだ後に対応する固定ファイルを原子的に置き換える
- 画面オーバーレイの表示倍率は`PUT /api/admin/screen-overlays/{slot}/scale`で`{\"scale\": 100}`形式を受け付ける。サーバーは1〜100の整数だけを、他の設定項目を変更せず`character.screen_overlays.{slot}.scale`へ保存する
- 表示設定APIは`screen_overlays`へ`top_left`、`top_right`、`bottom_left`、`bottom_right`の4キーを常に含め、各値を`{"image_url": URLまたはnull, "scale": 1〜100の整数}`形式で返す。各画面オーバーレイが存在する場合だけ、更新日時とサイズから生成する更新識別子を付加した同一オリジンの対応する`/assets/screen-overlay-*.webp` URLを返す。更新日時とサイズが同じ間は同じURL、どちらかが変わった場合は異なるURLを返す
- 表示設定APIは`drawing`を常に含め、`{"stabilization": 0〜10の整数}`形式で返す
- BGM管理APIはMP3、OGG、WAVのファイル名だけを受け付け、内容をFFmpegで検証・変換する。任意のファイル名や保存先は指定できない。
- BGMのアップロードは`POST /api/admin/background-music`のmultipartフィールド`audio`、削除は同じURLの`DELETE`とし、成功時は204を返す。拡張子は大文字・小文字を区別せず`.mp3`、`.ogg`、`.wav`だけを受け付ける。
- BGMのアップロード、変換、原子的置換、削除は同じ排他ロックで直列化する。音量保存ではこのロックを使用しない。
- BGM変換用の入力・出力には一意な一時パスを使用し、出力一時ファイルは`assets`と同じディレクトリに作成する。変換開始後は一時ファイルとBGM排他ロックをHTTP処理から独立した変換タスクが所有し、リクエスト中断時もFFmpegの終了を待ってから一時ファイルを削除する。
- FFmpegは最初の音声ストリームを必須として使用し、映像・字幕・データを除外する。標準入力を使用せず、出力先は既存しない一意なファイルとし、成功後に出力が空でないことを確認する。
- 表示設定APIはBGMが存在する場合だけ、ファイルの更新日時とサイズから生成する更新識別子を付加した同一オリジンの`/assets/background-music.webm` URLを返す。更新日時とサイズが同じ間は同じURL、どちらかが変わった場合は異なるURLを返す。設定された通常音量と発話中比率はBGMの有無にかかわらず常に返す。
- 表示設定APIは、同一オリジンの`/assets/`にあるVRMと待機・感情・食事モーションにも同じ更新識別子を付加する。外部URLは変更しない。
- `v`クエリを持つ`/assets/`の成功応答は1年間の`immutable`キャッシュを許可し、それ以外の応答は再検証を必須とする。

## 9. 設定項目

- `APP_CONFIG_FILE`を指定せずに起動し、`config.json`が存在しない場合は、同じディレクトリの`config.example.json`から`config.json`を生成する。生成時に管理用トークンとイベント識別子をランダム値へ置き換え、既存の`config.json`は上書きしない。
- アップデートでは既存の`config.json`の設定値を保持する。設定項目を追加する場合は既存ファイルを読める既定値を定義し、直前バージョン相当の設定を読み込むテストを追加する。食事モーションの入力欄は、以下の規則で新版サーバーの起動時に不足分だけ補う。
- LLM Responses APIのURL、APIキー、モデル名、システムプロンプト、食事反応プロンプト、検索中フィラー文の一覧
- TTSエンジン、APIのURL、話者ID
- 管理用トークン
- VRMファイル
- 待機VRMAファイル
- 感情とVRMAの対応
- 食事用Quadの右手ローカル位置、回転、大きさ
- 食事専用VRMAのURL、画像の消去開始時刻、食事演出全体の時間を`character.food_motion`で管理する。省略・`null`・空白だけの`url`は食事モーション未設定を表す。URLが未設定の間はVRMAを読み込まず、食事投稿を受け付けない。オブジェクトを指定する場合、`consume_at_ms`は0以上の整数、`duration_ms`は`consume_at_ms + 400`以上の整数とする。表示設定APIでも同じオブジェクトまたは`null`を返し、食事演出イベントの時刻とサーバー側の発話待機時間には投稿処理開始時の設定値を使用する。
- サーバー起動時に、使用中の設定ファイルで`character.food_motion`が省略または`null`の場合だけ、`{"url":"","consume_at_ms":3505,"duration_ms":14440}`を自動追加する。URLを入力する場所を用意するための未設定欄であり、時刻の初期値は`eat2.vrma`の記載例とする。`APP_CONFIG_FILE`で指定した設定ファイルにも同じ処理を行う。設定済みのオブジェクトには触れず、自動追加が不要な場合はファイルを書き直さない。
- 自動追加は元の設定全体を検証した後に行い、他の設定値と未知のキーを保持する。既存のアクセス権を維持して原子的に保存し、検証・保存に失敗した場合は元ファイルを保持して起動エラーを表示する。JSONにコメントや独自の有効・無効スイッチは追加しない。アップデーターは引き続き設定ファイルとアセットを置き換えず、必要な欄の追加は新版サーバーが起動時に行う。
- カメラ、背景、画面オーバーレイの各スロットの表示倍率、BGM通常音量、BGM発話中比率、照明、描画画面の手ブレ補正

管理画面から編集できるのは、LLM Responses APIのURL、モデル名、システムプロンプト、食事反応プロンプト、検索中フィラー文の一覧、TTSエンジンのURL、使用する話者・スタイル、固定スロットの背景画像、画面オーバーレイとその表示倍率、BGM、BGM通常音量、BGM発話中比率、描画画面の手ブレ補正とする。APIキー、管理用トークン、待受アドレス、FFmpegパス、背景色を含むその他のキャラクター設定は設定ファイルで管理し、管理画面へ値を送信しない。

## 10. 配布

- `v`で始まるバージョンタグをGitHubへpushした場合、GitHub ActionsでWindows x64版とLinux x64版をそれぞれビルドする。
- リリースタグは`Cargo.toml`のバージョンへ`v`を付けた値と一致する場合だけ受け付ける。
- 両OSでテストと静的解析が成功した場合だけ、実行ファイル、外部アップデーター、`web`、設定例、アセット配置説明をアーカイブし、SHA-256チェックサムとともにGitHub Releaseへ添付する。
- 配布物にはAPIキーを含む`config.json`、VRM・VRMA本体、FFmpeg、TTSエンジンを含めない。初回起動時は同梱した`config.example.json`から設定を生成する。
- 配布版の利用者はRustを導入せずに実行できる。
- Linuxでsystemdから起動する場合は管理画面からの自己更新を使用せず、サービスを停止して配布物を置き換えてから起動する。サーバーはsystemdの停止で送られるSIGTERMを受けて正常終了する。

## 11. 対象外

以下は本システムの対象外とする。

- ユーザーアカウント、個別ログイン
- 来場者ごとの会話履歴や永続保存
- LLMによる自由なモーション生成
- 音素・母音単位の精密な口パク
- MixamoやFBXモーションの実行時変換
